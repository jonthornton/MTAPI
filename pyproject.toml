[project]
name = "mtapi"
version = "0.1.0"
description = "MTA Realtime API JSON Proxy"
requires-python = ">=3.12"
dependencies = [
    "flask==3.1.3",
    "protobuf==6.33.5",
    "pytz==2025.2",
]

[dependency-groups]
dev = [
    "pytest==8.3.5",
]

[tool.uv]
package = false
